{% extends 'dashboard.html.twig' %}

{% block body %}
    <!-- Content Header (Page header) -->
    <section class="content-header">
        <h1>
            Empresa
            <small>ver</small>
        </h1>
    </section>

    <!-- Main content -->
    <section class="content">
        <div class="col-md-8">
            <!-- Default box -->
            <div class="box">
                <div class="box-header with-border">
                    <ul class="list-inline pull-left">
                        <li>
                            <a href="{{ path('company_index') }}">
                                <button type="button"
                                        class="btn btn-block btn-info btn-sm">
                                    <i class="fa fa-arrow-left"></i>
                                </button>
                            </a>
                        </li>
                    </ul>
                    <h3 class="box-title">Ver Empresa</h3>
                </div>
                <div class="box-body">
                    <div id="wrapper">
                        <div id="container">
                            <table class="table table-striped table-bordered">
                                <tbody>
                                <tr>
                                    <th><span class="pull-right">Id</span></th>
                                    <td>{{ company.id }}</td>
                                </tr>
                                <tr>
                                    <th><span class="pull-right">Nome</span></th>
                                    <td>{{ company.name }}</td>
                                </tr>
                                <tr>
                                    <th><span class="pull-right">Biografia</span></th>
                                    <td>{{ company.bio }}</td>
                                </tr>
                                <tr>
                                    <th><span class="pull-right">Categoria</span></th>
                                    <td>{{ company.category }}</td>
                                </tr>
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
                <!-- /.box-body -->
                <div class="box-footer">
                    <ul class="list-inline pull-right">
                        <li>
                            <a href="{{ path('company_index') }}">Voltar</a>
                        </li>
                        <li>
                            {{ form_start(delete_form) }}
                            <button type="submit" class="btn btn-danger">
                                Apagar
                            </button>
                            {{ form_end(delete_form) }}
                        </li>
                        <li>
                            <a href="{{ path('company_edit', { 'id': company.id }) }}">
                                <button type="button" class="btn btn-primary">
                                    Editar
                                </button>
                            </a>
                        </li>
                    </ul>
                </div>
                <!-- /.box-footer-->
            </div>
            <!-- /.box -->
        </div>
    </section>
    <!-- /.content -->
{% endblock %}
