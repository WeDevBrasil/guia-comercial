{% extends 'dashboard.html.twig' %}

{% block body %}
    <h1>Usertype</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ userType.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ userType.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('usertype_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('usertype_edit', { 'id': userType.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
